[project]
name = "finab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "finwise-python>=1.4.0",
    "python-dotenv>=1.2.1",
    "textual>=4.0.0",
    "ynab>=4.1.0",
]

[project.scripts]
finab = "finab.main:main"

[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
